<%= form_for(@lawn) do |f| %>
  <% if @lawn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lawn.errors.count, "error") %> prohibited this lawn from being saved:</h2>

      <ul>
      <% @lawn.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lawn_no %><br>
    <%= f.number_field :lawn_no %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
